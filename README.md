# CareHub – Full-Stack Healthcare Dashboard (Assignment 2)

A lightweight internal clinic operations dashboard prototype built with Next.js (App Router) + TypeScript.

## Getting Started

Install dependencies and start the dev server:

```bash
npm install
npm run dev
